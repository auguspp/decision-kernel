# R4-2 finite Sub2API experiment

Human authority: #526/5796637317 ("你用 sub2api 吧，预算不设限"). This supersedes the DeepSeek provider and CNY20 cap, not the two-sample/eight-call scope. Runtime baseline f956ea4. This experiment branch is not a new production route and is not intended to merge into main.

The two original immutable source archives are checked against explicit SHA256, source Git refs and page inventories. O is freshly executed on the same Sub2API/gpt-6-astra as S0/S1. Historical DeepSeek outputs are references only. S0 modifies only the route policy description in the same SDK schema; it is not a permanent method. Complete material, original SYSTEM, no tools/store, max_output_tokens6000, client retries0 and no fallback are shared. Existing Sub2API parameters omit reasoning; actual default and model weights are not certified. Actual returned model and usage are recorded; account billing is not invented.

Reuse: original model_request, typed models, FinalRequestCheck, official SDK, source decoder and Git create-only/uncertain-write primitive. A finite SDK evaluation driver is necessary because production full-input binding is DeepSeek-specific; production code, limits, requests and permissions remain unchanged. No new engine, queue, scheduler or daily selector. The script uses actual SDK no-network previews and checks exact actual request bytes before transport. All public output is saved before parsing; private reasoning items and credentials are never retained. O Quick requires this run's valid Pre. Invalid application output does not get repaired or retried; uncertain transport stops the batch.

A single create-only eval launch prevents repeat invocation. Results live only under research-eval/r4-2-sub2api-20260923-v1 and research_runs/evaluations/r4-2-sub2api-20260923-v1/, with raw/format/usage, exact source identity, original cutoff and current execution clocks. Native Git objects are read back; original research/day/slot/main/read-model are not written. GitHub artifact is also retained, including failure cases. Publication metadata containing its own commit hash is an artifact sidecar, not claimed inside that commit.

Before live execution: complete exact-head CI including mandatory installed-SDK/mock-HTTP tests and the unchanged full baseline. The live workflow must checkout that exact tested commit, use only the existing SUB2API_API_KEY and GitHub credentials in the execution step, require attempt1 and explicit approval, and never merge itself into production. This is not a model-quality or real daily Brief acceptance receipt. No R4-2.5, Full, Odds, financial acquisition or paid judge.
