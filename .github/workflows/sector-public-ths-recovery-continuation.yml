name: sector-public-ths-recovery-continuation

on:
  push:
    branches:
      - recovery/sector-public-ths-gap-cont-20260911

permissions:
  actions: read
  contents: read

concurrency:
  group: sector-public-ths-recovery-continuation-20260911
  cancel-in-progress: false

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout exact isolated continuation branch
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install existing Kernel and fixed AKShare reuse commit
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check -e .
          python -m pip install --disable-pip-version-check \
            'git+https://github.com/akfamily/akshare.git@8e95744b79ae22326308ccd2b4e62650c5b53c55'
          python -m py_compile .github/scripts/capture-sector-public-ths-recovery.py

      - name: Download exact failed HiThink-recovery source artifact
        uses: actions/download-artifact@v8
        with:
          artifact-ids: '10187281705'
          run-id: '34566950303'
          repository: auguspp/decision-kernel
          github-token: ${{ github.token }}
          digest-mismatch: error
          path: ${{ runner.temp }}/sector-recovery-source

      - name: Download prior partial public raw artifact
        uses: actions/download-artifact@v8
        with:
          artifact-ids: '10188840893'
          run-id: '34573861341'
          repository: auguspp/decision-kernel
          github-token: ${{ github.token }}
          digest-mismatch: error
          path: ${{ runner.temp }}/prior-public

      - name: Reuse six retained public raw files and request only remaining 205
        id: capture
        continue-on-error: true
        timeout-minutes: 15
        run: |
          python .github/scripts/capture-sector-public-ths-recovery.py capture \
            --source "$RUNNER_TEMP/sector-recovery-source" \
            --prior-public "$RUNNER_TEMP/prior-public/sector-public-ths-capture" \
            --output "$RUNNER_TEMP/sector-public-ths-cont-capture" \
            --spacing-seconds 1.0

      - name: Offline rebuild candidate without network
        id: verify
        if: ${{ steps.capture.outcome == 'success' }}
        continue-on-error: true
        run: |
          python .github/scripts/capture-sector-public-ths-recovery.py verify \
            --source "$RUNNER_TEMP/sector-recovery-source" \
            --capture "$RUNNER_TEMP/sector-public-ths-cont-capture" \
            --output "$RUNNER_TEMP/sector-public-ths-cont-verify"

      - name: Retain complete public raw set, candidate and receipts
        id: evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: sector-public-ths-recovery-cont-${{ github.run_id }}
          path: |
            ${{ runner.temp }}/sector-public-ths-cont-capture/
            ${{ runner.temp }}/sector-public-ths-cont-verify/
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 90

      - name: Enforce candidate-only acceptance without retry
        if: always()
        env:
          CAPTURE_OUTCOME: ${{ steps.capture.outcome }}
          VERIFY_OUTCOME: ${{ steps.verify.outcome }}
        run: |
          printf 'Capture outcome: %s; offline verify: %s\n' "$CAPTURE_OUTCOME" "$VERIFY_OUTCOME"
          test "$CAPTURE_OUTCOME" = success
          test "$VERIFY_OUTCOME" = success
