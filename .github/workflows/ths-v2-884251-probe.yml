name: ths-v2-884251-probe

on:
  push:
    branches: [probe/ths-v2-884251-20260911]

permissions:
  actions: read
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with: {persist-credentials: false}
      - uses: actions/setup-python@v7
        with: {python-version: '3.12'}
      - name: Install requests only
        run: python -m pip install --disable-pip-version-check requests==2.34.2
      - name: Download frozen source evidence
        uses: actions/download-artifact@v8
        with:
          artifact-ids: '10187281705'
          run-id: '34566950303'
          repository: auguspp/decision-kernel
          github-token: ${{ github.token }}
          digest-mismatch: error
          path: ${{ runner.temp }}/source
      - name: Probe 884251 over one exact THS v2 request
        id: probe
        continue-on-error: true
        run: |
          python .github/scripts/probe-ths-v2-884251.py \
            --source "$RUNNER_TEMP/source" \
            --output "$RUNNER_TEMP/v2-884251"
      - name: Retain exact raw and receipt
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ths-v2-884251-probe-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/v2-884251/
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 90
      - name: Enforce fail-closed probe
        if: always()
        env:
          OUTCOME: ${{ steps.probe.outcome }}
        run: test "$OUTCOME" = success
