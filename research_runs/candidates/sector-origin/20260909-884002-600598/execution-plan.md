# Bounded formal execution plan

Input `8c42719d50e00e60015cf2ad562446adec519def`, blob `0f276417e27c652e63ddb7ef3a1760c55b38e8dc`, canonical `346e1e9e1bb495b240c47f9cde5933829932e4c3d8721f76867758729f19f99a`. Do not overwrite or rename this input.

Call the unchanged `execute_after_admission` using connector-read exact Git byte/metadata projections. Refresh main immediately before launch. These adapters are bounded observations, not continuous HTTP callbacks or global locks. The source snapshot archive SHA256 is c92759834a595f38a35693d1fbe3129dc1146a67f8c125b4fe3ca5544e7aadbb; all87 manifest blobs checked locally; current compare381f4d1f..ac4e68d9 shows no changes to original admission, identity, Evidence, research model or Funnel. Local Python3.13/Pydantic2.13.4 is not the full pinned CI environment.

The callback begins formal execution and actually reads the retained source note. All subsequent container/Web invocations until sealed candidate count, including Pre construction, necessary Quick, validation and local readback. Reopen only the already obtained issuer narrative URL; do not repeat the failed PDF or homepage. Formal budget remains12 tool calls/10 reads/0 searches/0 retries/20min. Quick requires an actual Pre reason, not a wish to tick a box. No source-network request is inferred from a cached Web read.

Record local tool-return observations and public action summaries, not private chain of thought. Source preparation, old-input restoration, input commit/admission prerequisites and post-result Git publication are outside the formal span and disclosed. Preparation included an unavailable container-download route and DNS failure for an authorized Git source URL; exact old source blobs were then obtained through the connected GitHub tool and byte-verified locally. This is not a zero-failure full-session trace.

No runtime module, acquisition workflow or new Gate is added. Preserve old source failure and prospectively amended search scope. This case tests the saved Sector-to-company mapping, not latest company fundamentals or current valuation. No Human judgment, investment authority or Action is created.
