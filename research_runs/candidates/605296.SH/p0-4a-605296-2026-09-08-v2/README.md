# P0-4A v2: source acquisition incomplete, not completed Research

Status: **INCOMPLETE_SOURCE**. This attempt does not provide a complete Quick, WAIT, STOP,
Funnel disposition, quiet receipt, formal Research, or a Human decision.

## Exact identity

- execution_id: `p0-4a-605296-2026-09-08-v2`
- code: `a35dd0e6bb3ab942251ea78f018e0698b6541cf7`
- accepted saved reading: `e7ad9591a3139b568708a8d927494027d45da924`
- saved observation: Stock run `34167972382`, market session 2026-09-07
- new cutoff: `2026-09-08T14:03:18Z` (Beijing 22:03:18)
- input committed before acquisition: `fd0fb8f96ddff135530ee0c12a4b44fa5db4adbd`
- acquisition/research interval: 14:05:07.358332Z through 14:17:11.040122Z

The original two seed Evidence records are reused unchanged. Their historical retrieval
clocks are not this attempt's new retrieval claims. Existing retained ZIP source SHA256 and
Git blob identities were checked. Local v2 input bytes match the remote frozen input blob.

## Read these files at one pinned candidate commit

| File | Meaning |
|---|---|
| `input.json` | Contract, source-class gate, new cutoff, read-tool list and predeclared budget |
| `candidate.incomplete.json` | Initial Discovery/Pre continuation candidate, null Quick, original-model incomplete envelope and execution receipt |
| `audit.json` | Exact query/click arguments, durable resolved URLs where returned, request/observation clocks, errors, source inventory and provenance limits |

Budget limits: 48 acquisition calls / 12 search queries / 32 successful read operations /
1 technical retry / 25 minutes, all **SOFT_EXECUTOR**. Recorded use: 16 / 12 / 6 / 1 /
13 ceiling-observed minutes. Six successful reads mean two input/retained-observation reads,
three directory/form-shell reads, and one external government weekly-price bulletin body.
They are not six issuer reports. Preparation, local serialization and remote publication/CI
are distinguished from research acquisition; the receipt includes the research output stage.

## What remains missing

Formal H1 primary report, post-H1 monthly primary bodies and the cost/cash financial pages
were not read. Official industry context is price-only, without adequate supply/breeding
capacity coverage. The MOA historical July bulletin is retained as a bounded readback record,
not promoted into a complete source class, current issuer economics or qualified supplemental
Evidence. User-supplied and search-snippet financial numbers were not copied into FACTs.

SSE exposed a form shell, CNINFO catalogue access failed at DNS, Sina routes had decoding
errors/cache misses, and the H1 newspaper-summary body returned cache miss. Search and sole
retry allowances were exhausted without obtaining the mandatory classes. This does not
establish absence of the documents. It does not make an already-published report a future
market trigger. Full replay of HTTP/search returns or pre-cutoff source-version attestation
is not claimed; action summaries and platform reference strings remain distinguishable.

## Deterministic validation is a separate operation

The existing runtime supports incomplete candidates. This branch adds only a narrow test
`tests/test_p0_4a_v2_execution_gap.py` which parses the exact files, calls the unchanged
validator and actual existing schema/validation CLI, checks `EXECUTION_GAP` with null Funnel,
and prints the resulting validation JSON and hashes to the CI log. Existing success-candidate
scanner/tests, runtime, schema, Funnel and workflows are unchanged. The incomplete record is
not misnamed `candidate.json` or supplied with a fake `funnel.json` for the success-only scanner.
The CI result must be read at its exact head; this document is not itself a PASS flag.

## Acceptance boundary

User demand-side v1 verdict is preserved in #285 comment `5586338124`:
deterministic PASS / `FAIL_FOR_COMPLETE_RESEARCH`, product semantic classification
`INCOMPLETE_SOURCE`. All #285 v1 files remain unchanged. v2 is a new incomplete attempt,
not a repair that overwrites v1 or an accepted Research disposition.

No independent normal/adversarial real-executor pair was run. A pair must not be fabricated
from this one conversation or mocked outputs. Since required v2 research did not complete,
its after-v2 pair and demand-side acceptance remain unestablished. No declaration of overall
P0-4A completion. P0-1/P0-2 natural acceptance, P0-3 refresh and #263 remain independent.

This branch stays DRAFT, with no main merge, registry/handoff registration, formal Research,
Human wake/write-back, market dispatch, Re-run, automatic research retry, Deep/Odds or Secret
access. All investment, Human-attention and signal-transition authorities remain NONE.
