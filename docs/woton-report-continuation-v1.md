# Woton original-question report continuation v1

Scope: #297/5755964301, the Human's explicit "嗯，同意" in response to
#297/5755923127's precise proposal. This is not another blanket authorization.

Reuse Decision: **THIN_ADAPTER**. Reuse `stock_research_host.authorize`, native
Git/`saved_research_once.Retainer`, original `external_research_admission`,
`woton_report_custody.recover`, the native progress reader, original Pre/Quick
transitions and `stock_question_continuation`'s official DeepSeek wire. Internal
contracts were read at `ef87dadb008d49d0510786a550e2a8d35f04c566`. The same-capability
official Git/Responses and public PyGithub/pypdf/Requests audits in
5749609810 and 5753902322 are reused; no new package, parser, source transport,
model framework or scheduler is introduced. Existing license/privacy obligations
remain. This execution does not change generic source qualification or daily policy.

The one fixed logical child derives its execution ID from the original Woton
baseline ID and binds the exact original selection/failure plus all three r3
progress files. Its separate `stock-report-continuations/<parent-key>/v1/`
directory prevents changing the legacy baseline inventory/validators. Directory
location does not make it a new economic question. Any existing child file,
including partial output, blocks spending; create-only Git reservation and the
existing workflow concurrency protect the single intent. No force or overwrite.

Exactly the retained 133-page issuer-authored report via Sina is supplied.
`custody.recover` verifies/replays the saved PDF and representation, without any
issuer-site GET. The >1MiB PDF stays behind the existing manifest/native Git blob
reader; generic SourceRef512KiB remains unchanged. Full text must fit448KiB and
the actual original SDK prompt/schema must fit512KiB. Unknown issuer first-publication
stays `published_at: null` inside source data. The seeded Evidence is the newly
saved Git observation; its publication is proved by that exact Git commit,
not falsely assigned to the issuer. This reuses the original publication gate.

Only the related question, approved report and necessary execution binding enter
the model. Prior r3 body, parent failure, authorization comment and chat history
are verified locally and do not enter `pre_prompt`. Provider/parameters are the
existing official DeepSeek path: deepseek-flash, reasoning.effort=none,
6000 output tokens/stage, no tools/retries/fallback. At most one Pre and one Quick
only after the original validated CONTINUE_TO_QUICK. Before each stage, recheck
main/request/permission, parent and child bytes, input admission, exact prompt
and egress identity. Approval expires2026-09-23T00:00:00Z, with no auto-extension.

An isolated step in the existing Stock research job accepts only the exact
`woton-h1-analysis-ready` label on #297 by auguspp, main, attempt1. The label is
transport, not authority; runtime verifies the separately recorded Human scope.
The four-job inventory, source-only jobs and dispatch inputs remain intact.
The original research-job condition adds this explicit label branch; the legacy
baseline step is explicitly dispatch-only. Existing artifact retention is reused.
No Brief/task change,
new daily slot, market call, automatic Deep, investment or Human acceptance.

Native Git retains source context, preflight, input, reservation/launch, original
candidate/validation/receipt, README and host receipt, including real failures.
Raw provider inputs/output/usage remain in the original Actions artifact channel.
The existing on-demand archive index can register the exact child only after it
exists; saved/registered/published/read back and natural Brief/Human response
remain separate gates. No global catalogue or read-model mutation by the model.

Tests use original native admission/retention/Funnel with synthetic Git/model
boundaries, plus the actual retained PDF parser. Synthetic custody fixtures do
not certify source truth or constitute real Research. Full PR/main CI, actual
one-shot run, original artifacts, native Git recovery, independent semantic
review and normal publication must be checked separately. Implementing-model
review is not independent Human acceptance. P0 fresh-input acceptance stays open.
