# Human-first core reading — A1-H

Authority: #297/5847585258 and adopted NEXT-PHASE-CONSTRUCTION v2; Human: “好，开始做吧”. Start: main `a91c327d0d582bcda0f7680eb6142fb7315250fc`; reading `b5cd871d062089eaaaba43add860f36060590538`. Kickoff: #351/5847895052. This is implementation evidence, not another roadmap or a completed A1/A2 acceptance.

## User-visible increment

- Attention no longer repeats the complete Watch card below the same conditions. Explicit requests, price review, waiting and original resolutions still use #596; coverage failures show their reading impact, with original diagnostics expandable. Zero items remain bounded, not “nothing requires judgment”.
- Research Book starts with a searchable native table, ten registered objects per page. Only a unique registered prose record can provide an automatic excerpt, through the existing same-R byte/hash-verified readFile. The excerpt is an exact authored paragraph, not a new AI conclusion. Multiple versions are not resolved by date, filename, or title. A receipt-only object is not called Full Research.
- Company details open one object, keep the list behind “switch object”, and retain the order of corrections, research/conditions and version-specific Human records. Reading actions use purpose labels; paths, hashes, raw qualification and original source links remain under evidence details. Missing names/notes stay missing; no new company alias directory or research summary database.
- Odds Book lists retained calculations, saved prices with their original clocks, original review conditions, and limitations/response availability. It is not a current numerical Odds ranking. Missing/foreign-security prices do not acquire a condition; no proximity threshold, acceptance, position or trade is inferred.
- One full document opens in the reading area. Authored conclusion/section navigation and identified scope/limitations/counterevidence retain their text; full text remains accessible. Unsupported JSON is labelled unsupported and collapsed instead of made into a fictional summary. Existing conditional-worlds/commit-receipt display retains its narrow format checks. Decimal display uses bounded decimal digits/BigInt; approximate rounding is marked, tiny nonzero values remain nonzero, and original values are unchanged.

## Internal/official/external reuse

REUSE #585/#588/#596 reader, company catalogue, purpose/correction grouping, Watch semantics, original response binding, resume text, DOM controller and existing Node-to-pytest wrapper. reading.mjs, presentation.mjs and index.html are unchanged. Preview state is an ephemeral same-reading cache, never another source of truth; arrival updates only its cells so it cannot clear an opened original, search input or disclosure. Refresh clears it and old-R completions cannot populate the next reading.

Official: native table/caption/th scope and details/summary, textContent, existing Intl, and decimal BigInt. https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/table . No general grid, Markdown engine, DOM HTML execution or new dependency is introduced. Existing paragraph/heading navigation is not represented as full Markdown typesetting.

External code review (interaction reference only, not code copied or application installed):

- Vibe Research `85ba57191ba28cb9b4eeb4f7c1ed006d58d999c3`, `desktop/src/verticals/finance/pages/Notes.tsx`, blob `303400f3f256b0894fc50cc67cb98d75ea7b0bdb`: compact record rows, one opened long document, explicit actions and save failure separate from success. Do not import its React runtime, local ledger, deletion or agent API.
- EasyStock `3ae3a4e39d62643735dafe0c7f3c29950640adf9`, `frontend/src/components/MarketOverviewWorkspace.tsx` lines1–250, blob `2d194b44813fd5f9003a4c653a7d77849e13be39`, and `docs/market-overview-plan.md`: module context, independently retained source metadata and visible partial failures. A later search surfaced another ref; it was not blended into this pinned review. Do not import its scores, source fallback, automatic fetch cycle or desktop backend.

Reuse Decision: THIN_ADAPTER over existing saved products/native views. This is neither a new research interpreter nor a display-driven production contract.

## Verification and remaining acceptance

Current workbench baseline recovered and matched all nine exact Git blobs; its 58 Node cases passed before editing. Historical transport source.zip was recovered through existing artifact10907160579/run36244305114: all1216 file blobs and Git tree `3cb02d92e7a32f04d76cdd775cdbd033e3031b00` matched the independently fetched Git commit. The real97d→a91 compare plus current file blobs identified the required deltas. No source run was triggered for recovery.

The changed workbench runs75 Node cases, preserving all58 prior cases and adding17. Tests cover native lists, pagination/search, exact excerpts, multiple versions, bad sources, no inferred acceptance, decimal rendering, stale/late reads and no duplicated Watch. The existing pytest wrapper passes and runs that same suite once; its count is not another75. Two separate offline exercises read exact retained Xingye prose and Hengrui conditional-worlds bytes with the unchanged reader. Their transport envelope is controlled historical test data, not a newly published or current-R-qualified research package.

Initial regressions retained: the new Book prefetched the catalogue before an old test installed its failure override; the fixture now starts on the actual Attention landing page. The excerpt label/scope visibility regression was repaired without deleting its test. Preview completion was changed from whole-view rerender to cell-only updates, with an input/disclosure preservation test.

A local Chromium attempt to navigate to the preview was rejected with `ERR_BLOCKED_BY_ADMINISTRATOR`. That path was stopped; no browser policy was changed, no alternate origin used. Browser/layout/real phone and hosted Sites acceptance are NOT RUN here. Node DOM sinks and CSS rules are not screenshots or visual acceptance. Exact PR full CI, main/publication and Site adoption are separate receipts.

## Handoff and exit

Runtime delta is app.mjs/product.mjs only. Sites must compare current live adaptations and integrate the combined #596 + this increment where not yet adopted, preserving original root/compatibility URLs, owner-only access, CSP, layout and rollback. Do not blindly replace index.html/reading.mjs/presentation.mjs or declare a new deployment from the source ZIP. Verify actual desktop and phone reading, table navigation, source/receipt distinction, correction visibility, async original preservation and clipboard fallback in one consolidated update.

No provider, market/model request, scheduler, write service, permission, fee, Watch, Full, Odds calculation or investment change. The “read latest saved result” control stays read-only; contextual Quick and source refresh remain capability-dependent later batches. Optional view/cache and their dedicated tests retire with this workbench; original records, versions, responses, readers and shared integrity checks remain.
