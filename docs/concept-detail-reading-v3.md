# Saved concept supplement in the existing company reading v3

Continues Human Radar-r3 continuation and #297/5731908865 after the real
35355234987 supplement acceptance. Reuse Decision: REUSE + THIN_ADAPTER.

The existing publisher opts into `--include-concept-detail` only together with
Radar and primary concept reading. It listens to the existing detail workflow;
this changes read delivery, not market/model schedules or source permissions.
Legacy company v1/v2 calls keep their original fields and output semantics.
No primary/supplement acquisition module, source fingerprint or old raw byte is
changed. No new registry, score, trend threshold, provider or Research runner.

## Binding before composition

Read only the latest of at most20 main detail invocations. Validate exact run,
repository, workflow, main, dispatch, attempt1, code and clocks. No search for an
older success after a failed, active, malformed or expired latest attempt. This
is deliberately one saved batch, not a cumulative view of all batches.

Original Collector archive checks and installed `concept_detail_capture.verify`
are reused. Only bounded flat payload data is materialized. Artifact TARs are
inert and never unpacked or executed by this publisher. Embedded primary run,
code, artifact ID/digest and base ZIP bytes must match the independently qualified
primary in the SAME reading. Original request replay must reproduce the exact
supplement and original runner verification. Later primary data is not joined
with an old supplement; no stale fallback changes this boundary.

Every payload file and the full ZIP enter the original fixed Git read copy.
Existing252 API /128MiB and original blob reuse apply, with explicit preflight
reserve. A failed optional read rolls back only its new files/cache, not spent
calls, source originals or baseline Stock/Research context. Composition failure
preserves original company files and exposes a supplemental gap in the parent
reading. The original publisher still protects its read ref on overall failure.

## Company reading, not research admission

The existing company builder admits supplemental CURRENT_MEMBER origins before
its unchanged Stock/Research-context join. Code identity de-duplicates companies,
not observations; primary and supplemental origins keep their own request,
membership, received-at and projection identities. Multiple concept origins are
not multiple independent signals. Source names remain escaped data. BSE scope,
source-preparation failures, no matching record and old price dispositions keep
their original meaning; supported identity is not automatic Research admission.

Company v3 shows the original context and a separately labelled supplemental
section, including the acquired paths and gaps. `concept_companies` is the
primary-plus-supplement member union; primary and supplemental counts, overlap
and members outside the prior industry/institution/primary universe are separate.
The primary observation map is unchanged and PRIMARY-ONLY; its original387
unexamined count is not relabelled as a new source capture. Company page explains
this boundary instead of manufacturing an aggregated primary fingerprint.

## Acceptance and remaining work

Synthetic real-capture/Collector regressions cover newest failure, expiry, base
identity, tampering after rehash, partial history with retained membership,
unsupported identity, legacy/default output, wrong opt-in, source safety and
rollback without resetting consumption. The existing literal publisher-listener
assertion changes only to include the approved read-only detail trigger.
Exact-head full PR CI, independent main and normal publication/readback are
separate from local focused tests and real-data offline composition.

Actual six-concept/900-company counts remain historical sample expectations until
normal publication is read back; they are not900 Research tasks. Continuous and
cumulative discovery, selection quality, source-qualified Pre/necessary Quick,
wider Stock checks and other Smart Money dimensions remain open. No new dispatch,
retry, daily source activation, Research adoption, Odds or investment authority.

External reuse checked2026-09-18: GitHub official workflow_run and Git Trees docs:
https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows
https://docs.github.com/en/rest/git/trees
Trusted default-branch code executes; artifact bytes are not execution authority.
