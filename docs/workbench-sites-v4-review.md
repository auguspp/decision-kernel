# Sites v4 HTTPS return — scoped engineering acceptance

2026-09-26. Same #585 capability batch; authority #297/5836374143 and owner-only deployment authorization #297/5841761819. Product/requirements owner #351/#297, engineering Main Construction. This record supersedes the outstanding v3 host-blocker status, not the historical failures or full A1 acceptance requirements. Original review: [v3](workbench-sites-v3-review.md).

## Retained evidence, not another live browser session

Retrieved the user's named `Decision-Kernel-Sites-HTTPS-v4-followup.md` and `Decision-Kernel-Sites-v4-acceptance-evidence.zip` through Files/Library. ZIP: 443707 bytes, 22 members, CRC checked, SHA256 `78e34c284d7aa3703bf3ed0bb0642d7ba52fb66ecea9c41269b71e1c044041c2`. The package contains the prior OWNER_AUTH_REQUIRED receipt, follow-up, two actual desktop screenshots, original displayed bytes, clipboard/refresh records, manifests, complete v3-to-v4 and v2-to-v4 diffs, and exported deployment/access/rollback records. No archive instructions or QA scripts were executed.

- Site: `decision-kernel-progress`, version v4; version ID `appgprj_6aae41ff34688191a97e94d4c12f4832~appgver_b95b8da6be5081919a3d9eacf547a632`.
- Deployment ID `appgdep_6ab71c375a388191b6d9053873872abe`; returned status succeeded. Sites source `76192575483a0660b3a130d4bbbacfcc47747269`.
- Shared source H `ec0b760ddcfe4e608bcd5b19ede9984b40449032`; returned reading R `3c4818dc9ad7a068febb6889f53bf2546dccfeea`; embedded producer M `c847b4ad6fefa4b5d3c02536b4d02866a7c92258`.
- Returned policy revision 1: custom, 1 owner, 0 external visitors/editors/groups. This thread did not reopen, authenticate to, redeploy or change the Site.
- v2 rollback: version `appgprj_6aae41ff34688191a97e94d4c12f4832~appgver_f66f623b04848191b12c3ec44a2a9ae1`, source `4b8118ce3afaa34cb834d732d5062eaefde09576`, deployment `appgdep_6aae477b4b4881918b13cfc0f663a712`. Locator checked in returned metadata; no rollback drill or full archive restoration claimed.

Recomputed all four supplied source/test manifest entries against the exact recovered H bytes and Git blobs. Reconstructed the four added public/workbench runtime files from the cumulative diff: app/presentation match the Kernel originals; reading matches H (12275 bytes, SHA256 `7cea7d38580ffde44156eaf546ca5ca095b1a00a56a556ed5ce910ee427d2cfe`); the host index retains the v3 layout (7403 bytes, SHA256 `32fb5344f4558fe5d4afdddc1ada809efa054294f9b9d26951d9b073fbfff1c5`). This verifies returned diff consistency, not the entire Sites build artifact or current production bytes fetched by this thread.

The supplied displayed result is 13867 bytes, SHA256 `ef7b664dcebdff2e9688f1c96e47f5c3b4aea69462929f4fe65a2738fd902fc2`, matching the supplied exact-R descriptor check. It is one retained original, not all-library or canonical reading_hash verification. Reviewed both desktop screenshots; no newly captured browser evidence here.

## What the host evidence now establishes

Accept the Sites executor's bounded HTTPS observations: real catalogue (29 company associations), name/code/purpose searches, original byte/hash validation, desktop long-text scrolling, clipboard readback, same-R refresh, owner-only policy and locatable v2 rollback. The previous HTTP/Web Crypto and later owner-login obstacles remain historical, no longer current blockers for those successful paths. No production runtime change is needed for this reconciliation.

Direct isSecureContext/typeof values were not observable to that browser tool. Keep their diagnostic result GAP; do not interpret unavailable inspector output as false. Successful execution of the unchanged native-digest-required path establishes the primitive worked for the checked files, without fabricating a property measurement. See [W3C Web Crypto](https://www.w3.org/TR/webcrypto/).

The exact production response headers, HTTPS narrow viewport, controlled bad-hash injection, forced out-of-order completion and natural cross-R transition remain NOT VERIFIED in that browser. Module execution and returned document CSP are partial evidence, not a full header audit. Prior v3 narrow-screen evidence remains scoped to v3, even with unchanged layout. Do not corrupt canonical data, create an always-on debug endpoint, widen audience or demand another deployment just to manufacture diagnostics.

## Engineering verification and gap disposition

Five added native Node controller tests import the actual unchanged app.mjs and reading.mjs. A small output-only DOM double and synthetic Response promises control first/second completion, failure, navigation and cross-R refresh; native Web Crypto still validates the synthetic file bytes. They assert late success/failure cannot overwrite a newer selection, navigation/refresh invalidate old output, R-bound requests remain separate, and equal-length corruption yields an error without original/copy controls. This is source/controller integration, not a substitute claim of browser/CSS/auth/security-policy acceptance. No production injection seam or new package.

Existing 30 cases and these 5 pass locally on Node22.16.0; the single existing pytest wrapper executes the same 35 plus syntax checks (1 passed, not 35 additional tests). Removing both stale-result guards in a separate temporary mutant causes the four relevant cases to fail while the hash case passes. An initial missing input.value default in the test DOM double was corrected; it was test setup, not a product failure. The runtime files remain byte-identical to H. Full PR CI, merge and normal publisher results must be recorded separately after they occur.

Requirements disposition: the dependency-free, GET-only saved-reader engineering slice may proceed to normal Ready/full CI and, only with complete evidence, normal merge. Do not keep this slice indefinitely blocked on redundant property introspection after the actual operation succeeded. Do not mark outstanding browser diagnostics PASS: retain them here and in #351 for the next capable host check. A1 is still OPEN for useful curated Attention, complete object/market experience and host quality follow-up; A2 responses/holdings, L1 later research adoption and B remain separate. This is not a relaxation of source/hash/authority or CI requirements.

## Reuse, cost and next owner

Internal: the existing transport/hash tests, current stale-response guard, saved readers, CI wrapper and returned user evidence. Official: native Node test runner and controlled promises ([Node test docs](https://nodejs.org/api/test.html)); W3C Web Crypto; [Sites saved/deployed distinction](https://help.openai.com/en/articles/20001339-creating-and-managing-chatgpt-sites). Existing scoped Luna/gptme recovery/one-owner review remains applicable; no framework or host replacement. This adds only module-local tests and this receipt; their retirement follows the optional workbench consumer, not a new CI lane.

No source, model, schedule, authority, paid resource, workflow or host code changed. Keep v4 online under the user's existing permission; no fresh Sites task is needed for this test-only successor. The next product work should turn the proven reading paths into readable Attention/company views, instead of treating raw JSON and technical status labels as finished research UX. Replace the historical 'not production' badge during that next host update with a truthful read-only-trial label; do not redeploy solely for that label. Main Construction owns normal code integration; Human owns future host operation only when needed. No new investment or research acceptance follows.
