# Frozen Quick-only continuation — 2026-09-23

Scope: #297/5790359520 after #523. Reuse Decision: REUSE + THIN_ADAPTER.
This is one missing stage of an already admitted, frozen research question, not
another executor, automatic retry policy, new daily quota or investment authority.

## Existing capabilities reused

The original saved_research_once.research loop now accepts an explicit immutable
QuickCheckpoint. It rereads and revalidates the original input/candidate through
the original models and full validator. It preserves the exact Pre, Discovery,
evidence IDs, source references and original research cutoff. Only execution
identity/prefix, current code and one declared technical retry change for the
fixed existing technical-continuation-v1 child. No recursive child is accepted.

The existing stock_question_host dispatches the exact new mode from its trusted
main daily request. The thin stock_quick_continuation adapter binds the original
seven retained files and failed native run, current permission, current Git
scope and create-only reservation. It uses the same official provider builder,
complete-context transport hook, validator, native Retainer and result reader.
No workflow, dependency, source-acquisition tool or new scheduler is introduced.

Internal inspection: original host/executor, Stock technical continuation,
external_research_execution/admission/identity, reviewed_full_input and shared
capacity/reading. Official reuse: Pydantic validation and GitHub Contents exact
read/create-only behavior. Public prior art: LangGraph checkpoint/pending-write
resume implementation and documentation, inspected for successful-stage reuse
and idempotent side effects. Its graph/database runtime is not required by this
existing Git authority model; no external implementation is copied.

## Two clocks, honestly

The source preflight is revalidated through original prepare_input at the
ORIGINAL research cutoff. This is deliberately NOT the ordinary current
assess_admission path and cannot give it an expired-preflight fallback. The
adapter separately checks current main, request permission, exact input
readback, catalog ambiguity and launch ownership before one Quick attempt.
The old valid_until and all old source timestamps stay byte-identical.

The result and prompt say ORIGINAL_CUTOFF_NOT_CURRENT_PREFLIGHT / original
cutoff not rechecked as current. They do not assert the absence of new issuer
reports, current market qualification or fresh whole-issuer coverage. A task
that needs a newer evidence window is NOT this continuation. Historical source
qualification and current execution authority must not share a fabricated clock.

Referenced same-PDF notes are actually loaded, not merely passed through a lazy
loader constructor. Reuse the original note loaders and pinned Git trees to
compare original/current note inventory, blobs, bodies and clocks, including
at the send boundary. This preserves interpretation identity without reacquiring
PDFs or claiming a new independent economic review.

## Only the failed stage

No Pre model call occurs. The original pre.json is retained with an explicit
pre-reuse.json record in the native artifact; host/launch also bind its hash.
The receipt records a saved-Pre read instead of a new provider PRE event, and one
corrective Quick attempt as a technical retry. All attempts, raw output and
application failures use the original retention path. No claim kind, route or
counterevidence is automatically rewritten. Unknown causes remain unknown.

The fixed child is reserved create-only before model spending. Any existing
partial child blocks another invocation. Current parent/child bytes and request
permission are checked again at the send boundary; uncertain writes stop further
writes. Parent slot/day markers are not erased or recreated. Increasing the
technical retry budget of this child does not change the original daily policy.

The inherited complete-input SDK preview locally builds a Pre envelope to
establish the existing send hook; it is not a network request or Pre execution.
The only provider call permitted by this mode is Quick on the exact preserved
Pre and complete original context.

## Validation and actual configuration

Real original input, candidate, context, question, preflight, admission, launch
and receipts are negative checkpoint fixtures with byte/hash provenance. Native
Git and model responses in tests are synthetic, never real research acceptance.
Tests cover changed sources/clock/identity, no Pre call, honest retry accounting,
current permission and write races, repeated signals, another Quick failure,
full-source wire and original-reader parent/child round trips.

The daily request now selects this explicit corrective mode, bound to the
seven exact parent files and recorded continuation scope #297/5791078723.
Its fixture records this deployment without requiring future deployments to
keep the same ticker. Original daily-policy tests retain their source and
permission assertions on the exact archived parent when corrective mode is
selected; current corrective configuration and the exact saved activation have
separate checks. No assertion about daily quotas or source safety is removed.

Activation, one actual native run, semantic/financial review, registry and normal
Brief delivery are separate facts, not pre-certified by this document or CI.
Only after exact-head CI, normal merge, independent main CI and publisher/readback
may the existing daily label be used once. No automatic Deep, Odds or trades.
