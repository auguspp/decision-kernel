# P0：同一观察日的两名成员历史比较

需求与授权：#297 评论5613831635、5613905088、5613953868，以及随后Human批准按此执行。长期需要的是可追溯的成员历史比较；GitHub手动触发只是当前执行环境的薄入口，不属于Kernel语义。本切片不增加研究准入关卡。

## 本次范围

固定2026-09-09、粮食种植884002.TI的完整两名当期成员：北大荒600598.SH、苏垦农发601952.SH。共同5/20/60交易日，参考原保存的行业及沪深300序列。原run34364727989/artifact10109789379和其摘要在`radar_inputs/sector-member-reading-v0.json`精确绑定；不更新行业，不把9月10日盘中报价混入9月9日比较。

复用`current_state.unpack_archive/validate_sector`、原market-state解析、`hithink_stock_reading.history_params/action_params/request_json/qualify`及`stock_radar_reading._stock_path`。只补四笔获取及比较呈现，不新增供应商、复权算法、评分器、角色分类平台、数据库或依赖。

请求严格为每公司一笔61根日线历史与一笔截至原观察日的已报告公司行为，共最多4笔，间隔20秒，零源站重试；原交易日历、行业/基准和截止日快照全部复用已保存包。第一次失败即停止后续请求，保留安全的已取得响应和明确failure，不让剩下一家公司自动获胜。

截止日快照来自原全市场分页响应中的实际行，保留原response路径、摘要及接收时钟；派生单行投影不冒充新调用的单股端点。按原校验对比OHLC/成交量/前收，成交额仅沿用已有有界容差。新历史在现在取得，不声称过去实时已观察、历史源文完全不变或具备盲测独立性。

## 价格与角色的边界

原reader只支持原始价格，不自动复权；报告的公司行为穿越5/20日时原输入资格拒绝，穿越60日时该窗口不显示股价收益、超额、回撤或名次。某一成员不可比，则同窗全组排名均为空，不能用未复权断点强行排leader。供应商未报告行为也不是穷尽性无行为证明；不把原价比率称总收益或已完成的复权研究。

输出含共同起止日、成员累计收益、相对行业/大盘差值、组内区间名次、固定基期累计跑赢行业的天数、收盘最大回撤、平均成交额及完整两成员成交份额。固定基期累计超额天数不是滚动胜率；收盘回撤不等于盘中最大回撤。角色保持未定：这些字段是后续解释弹性先锋/中军/跟随的依据，不是自动标签；缺流通规模、真实指数权重和分钟先后关系不补造。

## 运行与验证

入口`.github/workflows/sector-member-reading.yml`只有`workflow_dispatch`，没有schedule/push/CI事件替代触发。只允许main/attempt1；沿用股票任务并发组，并在执行前检查活跃共享HiThinkKey的Sector/Inbox/股票任务。该检查是实际启动前快照，不是跨工作流原子锁；其他流程可能之后启动，不能宣称全局锁已经建立。活跃列表截断或有冲突即停止，不自动重试。

在GitHub Actions中选择`sector-member-reading`、main、Run workflow即可；无股票代码或来源参数需要Human搬运。调用回执不明先查运行列表，禁止重复点击或Re-run。代码合并本身不会触发这4笔行情。

成功后同次无凭据`verify`从保存响应重算；保留原ZIP、来源元数据、派生quote、每笔请求/响应、结果或失败和verification，90日artifact。实际阅读入口是该run的summary及artifact/capture/summary.md。文件存在/本地测试/CI通过均不等于真实数据已经取得，角色或原公司选择已通过验收。

后续将这次真实比较及局限交付回#299并绑定新保存版本；旧研究input/candidate/Funnel、旧失败、原选择记录不改。完整角色解释、日常Research、原生Scheduled、Human决定和5–10交易日实用检验不因本切片签完成。
