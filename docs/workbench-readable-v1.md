# A1 readable workbench — saved content, not new research

2026-09-26. Product #351; scope/authority #351/5842481076, original start #297/5836374143. Human reports the Site root replacement complete and authorizes continuing the existing plan. Treat it as Human-reported completion, not an independently verified deployment ID/diff. Do not revert the root route, bring back the engineering homepage, or ask for the same change again.

## Goal and actual delta

On top of #585's verified saved-reader, put retained research and company uses ahead of technical plumbing. Manual `MANUAL E2E HOSTED QUICK` is a visible research record, distinct from scheduled Quick, Brief, supplements and other comments. Exact known first-line headings determine display roles only. They do not validate economics, accept research, imply today's coverage, establish a pending Full commission or override corrections. Retain all records, their separate clocks and the bounded page range.

The overview exposes the exact first paragraph of an explicitly titled conclusion section, labelled an excerpt, alongside the full analysis, counterevidence and limitations. This is not a model-generated summary. Unknown headings retain a full-text entry; fenced examples cannot become conclusions. Markdown remains literal, arranged into native headings/paragraphs; no general Markdown engine, HTML parser, arbitrary rendering or sanitization bypass is added. Full source text remains separately accessible.

Saved Watch prices/conditions gain readable labels and a link into the same company's catalogue. No price fetch, proximity threshold, Odds calculation or buy signal is introduced. Company assets use their registered purpose, retain the original note, show historical acceptance/status separately and never infer holdings or a final research version. Search can carry a ticker from Watch. A missing catalogue remains a gap.

Selected byte-verified originals gain a narrow field view for the existing probability-free conditional-worlds v1 format and research-commit-only v0 receipt. Conditions, price/time conventions, old acceptance, limitations and original decimals remain. Unknown, malformed or mismatched formats fall back to the complete original, never invented probabilities/units/current conclusions. A commit receipt is explicitly not research prose. Narrative originals retain exact raw text behind the paragraph view.

Markets offers named links to existing registered summaries and keeps the full original index. Ambiguous/missing summary locators are not silently selected. This is a navigation improvement, not new cross-market coverage or a new sector interpretation. Times with explicit zones are displayed with native Intl in Asia/Shanghai; their raw values remain in provenance. Date-only/unknown inputs are not silently assigned a clock.

## Reuse, ownership and scope

- Internal: fixed initial M212f44120dad02119a94d07b76561b2fb6207a19 AGENTS/plan/#297, #585 app/reader/presentation/controller tests, actual #575/5829920878 heading and content, exact R55c738280e2b5e84a92313fe722938b05a49c91f catalogue and selected conditional result/Sector summary. Shared transport, byte/hash/allowlist, stale-response guards and producer schemas remain unchanged.
- Before committing, main advanced to 063f7620f04ca21840f63aebb832517f8c3f8db9 via independent #586. Actual compare shows no changes to this workbench, its tests, AGENTS or construction plan. A second bounded compare to 325318113711c79ec880bbcb99c5cc346ad6c24c adds only the #587 Smart Money composition repair/test, again no overlap. Base this batch on 3253181, preserving all Smart Money code and its separate acceptance. Do not touch its workflows or publisher.
- Official: native DOM textContent/details and Intl.DateTimeFormat. Sources: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/DateTimeFormat/DateTimeFormat and https://html.spec.whatwg.org/multipage/interactive-elements.html#the-details-element . No HTML execution or new request surface.
- External: TexteaInc/json-viewer@2ecef6dd335daf5903519315c1829c0894a915c2 src/index.tsx inspected: type-specific views, collapse and original-data access are useful; a generic JSON editor/tree does not supply this project's research/price/acceptance semantics. Do not reconstruct an editor or copy its editing authority. Marked's official options/security guidance was compared (https://marked.js.org/using_advanced); full Markdown rendering would need a deliberate safe adapter. No home-made general replacement is introduced here. Formatting syntax may remain visible; native GitHub originals remain available.
- Reuse Decision: THIN_ADAPTER on existing outputs/native UI, not a new renderer platform, state database, Agent, scheduler or validation policy. Existing Luna/gptme one-owner/thin-host review remains applicable; it is not repeated as a new integration.

## Verification and known limits

Local recovered workbench files were checked against exact Git blobs. Node22.16.0 runs 48 cases: existing35 plus11 pure product projections and2 actual app/controller scenarios. The single existing pytest wrapper runs the same48 plus syntax, not another48. Actual manual-heading visibility, literal hostile source text, timezone/date handling, conclusion extraction, unknown formats, unchanged conditional values, and ambiguous locator behaviour are covered. Existing stale-result/hash/environment tests remain active. Initial paragraph rendering dropped a terminal newline from displayed content; the original raw-text disclosure was added, preserving exact text instead of weakening the identity tests. Those intermediate failures remain in local logs.

This is Node plus minimal DOM sinks and synthetic transports, not a real browser/CSS/clipboard/security or whole-repo execution. No live Site was opened or altered here. Full PR CI and host validation are separate records, only after they occur. A1 overall (including broader Attention curation), A2 responses/holdings, L1 actual later research adoption, and B world observation/calendar are not declared complete. The reported root-route change is preserved without guessing its version.

## Handoff, cost and exit

Sites should update app.mjs and add product.mjs, keeping the current root route, compatibility URL, owner-only policy, layout and CSP. No old index.html replacement. New styling is scoped/native text and adds no network endpoint. Validate the actual touched paths in one host update: manual Quick visible with full limits, company link/search, original/receipt distinction, conditional results with prerequisites, market-summary opening, and phone readability. Do not repeat unavailable diagnostic probes or ask for a new Site. Preserve current live rollback and return real version/diff.

No new dependency, recurring fee, producer, model call, notification, workflow or data write. Optional product projection and its tests retire with the workbench; historical research/decisions, #585 reader protections and other consumers do not. Ordinary PR rollback only. Engineering regression of this real display omission is not proof that the research Learning Loop has improved.

## Successor: attention and response continuity (A1/A2, 2026-09-26)

Authority: #297/5847200266, following #297/5847120007 and Human's explicit
mainline instruction after Smart Money v1.1. Base M c99c0508d550c620b33cbbb334db46a91c24c886.
This is a bounded workbench increment, not completion of all A2/personal context.
The Smart Money branch stays closed. Do not change its sources, schedules or gaps.

The existing Attention page now distinguishes explicitly registered pending
handoffs, saved price triggers requiring review, waiting price conditions, and
exactly bound historical dispositions. Quick remains separate research content.
Missing/duplicate/contradictory request identities and unavailable prices are
visible coverage gaps, not extra Human chores. A zero visible count never claims
complete personal/workflow coverage. No prose classifier or new task-state store.

Selecting an item carries its exact request id or registered Watch identity into
the existing company catalogue. Corrections/method restrictions, original research
and condition versions, historical Human checkpoints, and other material are
ordered by existing purpose fields. Same-company grouping is not supersession or
transfer of acceptance. Original request and resolution remain distinct byte-checked
sources at the same R, even when the optional company catalogue is unavailable.
An old Watch with a null source_ref is resolved only by its exact registry id,
source path and original Git blob; it is not replaced with today's same-named file.

Copying a continuation produces a recovery request with immutable R, exact item,
original observation clock, source hashes, linked disposition and company versions.
It explicitly contains no new Human response. There is no send/close/accept button,
automatic comment write, browser credential, persistent private draft or position
inference. A real later response still uses the existing interactive GitHub comment
protocol: restore the target, reconcile duplicates/uncertain writes, preserve the
actual words and exact readback. Engineering approval is not investment acceptance.

Catalogue completion no longer clears an original the user opened while the
catalogue was loading. Actual navigation or an R refresh still invalidates stale
reads; refresh clears selected-item context rather than mixing the old request
with new research. All rendered source text remains literal textContent.

Reuse: existing project_handoffs/external identity isolation, watchSummary,
readAssets/readFile, purpose references, resumeText, DOM/controller tests and the
original response protocol. WHATWG details/summary semantics were rechecked; the
json-viewer/Luna/gptme comparison recorded above remains applicable to this exact
native display/one-owner scope. No new library, renderer, index, state schema or
writer service. Remove these projections and their tests with the optional
workbench; do not erase source histories or shared reader protections.

Local checks cover malformed sources, same-security different-version requests,
conflicting inventories, saved trigger/wait/unknown separation, exact Watch origin,
copy without fabricated response, Quick/catalogue failure isolation, real controller
navigation and cross-R refresh. Synthetic replies are fixtures, not real Human
acceptance. A malformed null pending row and catalogue/render race were found and
fixed during these tests; the original transport/hash/stale-response guards were
not weakened. CI, host deployment, actual new Human response and later independent
recovery remain separate acceptance records, not implications of tests passing.

Sites handoff: replace only app.mjs and product.mjs together; leave index.html,
reading.mjs, presentation.mjs, root route, old links, owner-only access and CSP intact.
Keep the current deployed rollback. Check grouped Attention, item-to-company,
original/resolution reading, copy text, delayed catalogue, missing data and phone
readability on the host. The repository merge alone is not a Sites deployment.
