# Published Industry origin capacity and retained-context re-entry

Human continuation and pre-construction three-layer Reuse Check: #297/5787747772.
Base main d21fb0e4d074926e81c0fca221b28324010b2a65. Reuse Decision: REUSE + THIN_ADAPTER.

## Remove the demonstrated small-report bottleneck, not source identity

The saved report at c32fdf6c9d6349cfe7f82ef8b9dd18cc1ed850ac,
`details/radar/industry-breadth.json`, is573486 bytes. Native preparation
35808185505 stopped at INDUSTRY_REPORT_SIZE after retaining the complete
898366-byte issuer context. That was not a missing PDF or failed CNINFO query.

The original strict JSON/source helpers now give this exact published origin
path AND purpose SAVED_INDUSTRY_BATCH_ORIGIN a4MiB allowance, eight times the
old512KiB. Native report decoding uses the same ceiling. The complete original
bytes, Git blob/SHA256, same pinned reading, native archive/replay, dates,
reviewed batch and host admission still bind. This is a storage allowance,
not permission and not a model prompt. Other source purposes and default
execution/authorization JSON remain at their original512KiB boundary.

GitHub Contents omits inline base64 above1MB. A thin subclass of the existing
GitHubAPI, used only by source preparation and the original question host,
requests the exact native Git blob for this Industry path/ref, up to4MiB.
It validates path/type/size/SHA, strict base64, decoded bytes and Git blob identity;
no returned download URL is followed and no credentials go to another host.
Original inline reads and the existing8MiB API JSON budget remain unchanged.
The shared capture/publisher client stays byte-identical: its unrelated change
would invalidate historical capture fingerprints. No historical fingerprint
or compatibility map is changed to make these tests pass.
The4MiB choice leaves representation growth room inside that proven transport
budget; unbounded untrusted JSON would remove memory/CPU protection without
solving source admission. This is not a provider's token limit.

The existing full issuer context is898366 bytes, inside its reviewed1.5MiB
decoded/2MiB actual-request contract. No model byte/output/time allowance, live
rate limit, source identity, stable question/day record or Human authority is
silently changed. Further demonstrated capacity mismatches should be fixed
end-to-end with measured margin, not as repeated single-if patches.

## Continue after the saved checkpoint, not from the beginning

The main-owned plan names exact failed run35808185505/artifact10728662647,
its original failure receipt, prepare, prior continuation and context refs.
SINGLE_RETAINED_CONTEXT_ORIGIN_REPAIR accepts only the known pre-query size
failure with zero model/Research/PDF-acquisition/query counts, known writes,
unchanged base plan and the exact three-file original prefix. It appends
origin-continuation.json CREATE-only before resuming the original preparation.
The existing context is read and validated rather than rebuilt or overwritten;
current source custody still replays every original PDF/page before input-ready.
Extra outputs, a prior new marker, changed bytes or uncertain writes stop repeats.
The original first-failure continuation contract remains usable and tested.

No new source or model workflow, scheduler, dependency, retry loop or provider.
No actual daily Research request is activated by this PR. Original financial
correction inventory remains necessary; an unreviewed correction cannot become
STATIC or a no-update assertion. Source preparation, formal execution, Research
quality, normal publication and Human delivery remain separate facts.

## Evidence and tests

Offline regressions exercise the original host with573486-byte and2–4MiB
synthetic reports, purpose isolation, strict JSON, changed source bytes, native
GitHub encoding-none replies, exact checkpoint proof, duplicate/uncertain writes,
retained-context custody and a two-report source-only composition. Synthetic
padding is not a captured real report. The real failed receipt is an exact-byte
fixture; the old main recipe is frozen for old continuation tests, not rewritten
history. Every prior5813 test identity must survive complete exact-head/main CI.

Official and external reuse inspected: GitHub Contents and Git Blobs REST docs;
Python json object_pairs_hook and resource-limit guidance; PyGithub ContentFile
and GitBlob implementations; hub4j/github-api issue1558's encoding-none failure.
Existing requests/strict JSON/Git/Retainer are reused without adding those SDKs.
Actual live success must be recorded after main CI, normal publisher/readback
and one explicit preparation signal; this document does not pre-certify it.
