# Concept first-source failure: retained diagnosis, not a successful scan

Refs #297/5727223500; Radar-r3 / #433. Reuse Decision: REUSE + THIN_ADAPTER.

Run35322113586 / attempt1 / codeaae13ecc56c2361e94a6249ea860693af7206923
finished as failure on2026-09-18 at08:00:42Z. Actual artifact10537911612 is
3,036,875 bytes, SHA2568851ab88c6cf9b41101a25baaa1d9cec017f4c8bfd0dfb75748306b64753c381.
The actual original851-file Git tree was reconstructed as62b93ba5635bd227b697112a644ea2bf8abfd64c.
Its original verifier ran offline: retained failure verified, remote cause NOT re-proven.

Calendar and cn_concept catalog requests succeeded (HTTP200/code0);390 unique
concept identities were retained. Request3, the industry catalog, raised
DumpTrialError during response qualification. No snapshot, benchmark history,
theme detail, member/company output, Stock check, Pre or Quick was produced.
A directory is not390 opportunities or completed trends. `industry` is a
documented valid tag in the reviewed upstream catalog contract; do not guess a
parameter correction or blame credentials/timeout/429 without retained evidence.

The exact original call path narrows the failure to `_check_response`: HTTP status,
content encoding or content length rejection. The concept wrapper discarded that
existing finite error code and optional HTTP status. Historical specific code,
status and remote cause remain UNKNOWN. The old artifact is never patched.

The correction reuses DumpTrialError and existing reason_code/http_status fields.
Only HTTP_REJECTED, UNEXPECTED_CONTENT_ENCODING and INVALID_CONTENT_LENGTH survive
as finite diagnostic codes. An actual finite response status is preserved, with
its local failure-observation clock, even when the rejected body is deliberately
not retained. Unknown/inconsistent exception fields remain generic; exception
text, headers, remote URLs, bodies and secrets are not copied as diagnostics.
Offline verification checks structural status/reason/clock consistency; it cannot
authenticate a historical HTTP status solely from a rehashed failure receipt.

No route, request parameter, status acceptance, clock window, size/request budget,
retry rule, workflow trigger, source selection or Research authority changes.
The original capture format and policy remain; implementation fingerprints still
bind the exact source version. Original artifacts must use their original code;
new code cannot silently certify old fingerprints. Success still rebuilds from
all original response bytes. Tests exercise the actual request_raw/_check_response
seam with synthetic response objects, including no read of rejected/unsafe bodies.

This fixes diagnostic loss, not an identified remote provider defect. A further
live attempt needs separately reconciled explicit permission; no rerun or next
source request is triggered by this patch. Full continuous concept trends,
company-reading integration and research value remain unaccepted.
