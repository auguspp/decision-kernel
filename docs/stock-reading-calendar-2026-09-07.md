# 股票手动读取：先核验日历，不以星期几禁止尝试

此前在 `c73f4dcc929a8cd3181bdb48875f584d9fcd5966`，股票计划/采集/重建复用 Theme 的当天或相邻周末 `_window`。2026-09-07 周一早晨，即使周五仍为最新完成交易日，也在真正日历请求之前被拒绝。这是实现限制，不是用户无权测试或 HiThink 密钥失效。

本次仅修改股票 reader 与 capture/replay 时钟调用：准备有界计划前检查真实时区和保存行情已完成；首次原有 HiThink 日历响应到达后，用已有 latest_completed_a_share_session 确认保存状态仍为最新完成交易日，并检查完整状态窗口及日历覆盖当前读取日期。此后每次请求前、响应后和最终输出继续核验，跨收盘不继续拿旧状态发布。日历没有覆盖读取日期不是节假日证明，缺中间交易日不桥接；仍须生产路径的 qualified recovery。

不修改共享 Theme/Sector 规则、不增加强制开关/新供应商/自动恢复/触发器。不新增请求，26次上限与30分钟计划寿命不变。当前报价、前收、公司行为、61日、身份、凭据、完整计划、来源哈希和所有 authority 检查不变。指数快照自身的现有资格限制也不在此改动中放宽；通过日历只允许继续取数，不保证实际快照和个股数据随后必定通过。

原有股票测试fixture补入明确合成的未来日历日期；断言不删除。新增周一完整capture/replay、合成节假日、日历覆盖不足/缺日/请求失败、已完成中间日、跨收盘、重哈希篡改、真实时区反例。所有行情均是合成且禁止网络，不能称为真实股票标的。旧版本附件按其精确代码重建，v3不静默复用旧v2结果。

下一次实际股票工作流仍使用用户已有GitHub HiThink secret，选择main、stock-reading和精确成功Sector run。33939414197只标识2026-09-04旧状态；是否仍可用由该次真实日历与现有行情检查判定，不把它写死为默认值。未执行云端workflow_dispatch就没有新真实股票结果，CI通过不等于实际取数或股票附件验收。保留SHADOW OBSERVATION ONLY及Human/Research/Investment authority NONE。
