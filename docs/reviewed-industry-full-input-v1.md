# Complete Industry question input through the original host

Scope: #297/5786165647, original Human Industry/DeepSeek approval5778160521,
implementation clarification #519/5786704437. Reuse Decision: REUSE + THIN_ADAPTER.
Base main a1a42eb44cf462bb7890d078817699582fa9eae8.

## Existing mechanisms, explicit new consumer

The existing bounded zlib/base64 codec, native artifact reader, PDF parser,
same-PDF reader, SDK request hook, question host, admission and Retainer are
reused. Historical Stock603353/300711 wrappers retain their policy/ticker scope.
No new codec, provider, scheduler, database, dependency or research loop.

REVIEWED_INDUSTRY_FULL_INPUT_ZLIB_V1 stores at most512KiB, decodes at most1.5MiB,
and checks the actual SDK request at2MiB. These are byte limits, not guarantees
of a provider's token window. Compression is storage only: complete plain
issuer text reaches the original Pre and only a necessary validated Quick.
Original small inputs remain unchanged. Truncation, malformed/concatenated
streams, changed bytes, wrong identifiers and excess expansion are rejected.

An actual SDK preview is built before any reservation with transport forbidden.
The original final request hook checks destination, model, reasoning, output
schema, complete plain input and one send. The original daily ledger, stable
question root, duplicate checks and launch-time admission remain. No automatic
Deep, repeated spending, monetary ceiling, Odds or investment authority.

## Original source custody and page replay

The existing import registry explicitly names run35754054446/artifact10706728112
and all12 original file identities. The event remains issues; no fabricated old
Stock inventory, journal or workflow_dispatch. Original capture plan, source
manifest, PDF/extraction Git bytes, printed identity and source clocks are checked.
The older Zhenjiang format retains its own contract while sharing only the
existing native artifact-qualification helper. This new format currently accepts
these reviewed CNINFO originals, not arbitrary mirrors or URLs.

SAME_PDF_REPLAY_REFERENCE_V1 avoids placing the same full body twice in the model
context. Every page text remains present once. The exact reading hash and frozen
review commit bind the proof: custody rebuilds the entire same-PDF representation
from original PDF bytes, checks that hash, current-main note equality and every
plain page. No hash-only source admission. Old inline proofs remain supported.
Immutable commit metadata is shared within one loader; current-main note text,
authorization and consumption remain checked on re-entry. Original source and
review clocks are not retimed just because implementation main changed.

Dates keep DAY_NOT_AVAILABILITY_TIMESTAMP. Normalized midnight is not proof of
first availability. Retrieval, Git custody, review and research cutoff differ.
Necessary LATEST_INVENTORY checks cannot be downgraded to STATIC.

## Same saved question, first formal invocation

A complete Industry input with qualified Industry origin can retain
CONTINUE_ANALYSIS and exact RETAINED_ANALYSIS_PREDECESSOR references for its FIRST
formal host execution. This is input qualification, not authority. Original
catalogue and durable question/day markers still reject a prior formal attempt.
Default small inputs and other continuation kinds retain the original rejection.
Do not rename the question NEW_DISTINCT merely to launch it.

## Explicit source-only preparation

The interaction's container lacked DNS and could not export the complete pinned
reading/19 note files. The existing source-only workflow job therefore also
accepts industry-question-input-ready after normal main-CI/publication gates.
A main-owned recipe reads the already-retained artifacts/notes, freezes the same
question and then queries the declared report-correction window. No PDF is
reacquired, no model credentials are supplied, no research day is consumed.
An unresolved correction body withholds preflight success. The create-only
preparation prefix is never overwritten/retried after an uncertain write.
The proposed daily request is data until separately reviewed into main.

The current recipe continues jiangxi-copper-net-economic-exposure, two financial
reports and possible corrections in2026-03-27..2026-09-23. Other temporary
announcements remain deferred per Human report-priority scope, not falsely
reviewed. Existing day/question policy and all old failures remain.

## Verification

Tests reuse original Industry/Stock fixtures, real PDF reading, original
admission/Funnel/Retainer and pinned SDK-built requests; only I/O/model edges
are synthetic. Full unchanged5705 baseline identities, exact-head/main CI,
normal publisher/readback and actual source/model runs are separate evidence.
No test or documentation pre-certifies current correction inventory, formal
Research, natural delivery, Human acceptance or complete P1.

Reuse evidence: original stock_full_input/bridge, prior external audit5656635695,
docs/readings/stock-full-input-adapter-2026-09-14.md, official Python zlib bounded
decompression and HTTPX request hooks, existing pinned SDK/DeepSeek adaptation.
