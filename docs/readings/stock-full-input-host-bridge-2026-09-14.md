# Stock full-input original-host bridge — 2026-09-14

Continues #297 Human permission5652950925 and pre-construction Reuse Check
5656929743, after #357's exact-head full CI3084passed and merge0762a21e.
THIN_ADAPTER / COMPOSE_EXISTING_HOST. This is not a successor or a live run.

## Implemented composition

`stock_research_host.run_item(full_input=True)` internally composes the original
complete source preparation with #357's lossless storage and original Retainer,
checked-source, Evidence bundle, preflight, admission, research(), original SDK
model_call and validators. It is scoped to603353/300711. The default is false;
consume(), CLI and workflow do not enable it. Old source-recovery-v1 cannot opt
in. Existing history still stops preparation before any new source/model work.
No fresh execution identity is manufactured to make those histories disappear.

The original source preparation is called once. All selected bodies and visual
reviews must be present; prepared-context byte/hash and inventory must agree,
and original journal source clocks are retained. The original capacity-failure
record is not overwritten. A separate local representation record is not source
truth certification or execution admission. Missing pages still fail closed.

A BoundFullContext retains both the exact stored envelope and exact decoded
bytes, using the original512KiB checked-source. It binds the one MODEL_CONTEXT
source, ticker/security/lane and seed stored-artifact hash. Source truth, PIT and
permission remain the original gates. The default plain-context hash equality
is unchanged. The full path does not replace that equality with an arbitrary
callback or an unverified decoded hash supplied by a caller.

## Pre-launch and actual-send checks

The ORIGINAL complete Pre prompt and response parameters are factored into
shared functions. The Pre literal is AST-identical to the old prompt. Preview
and actual model_call share all original body/schema/strict-format size checks.
Before launch, the original SDK constructs the exact current Pre HTTP request
with a noncredential key, request hook and mandatory deny transport; it never
sends. The checked prompt/wire hashes and stored/decoded identity are recorded
in launch.json. A preview is not an admission token or model result.

The original model_call has an explicit bound-context argument, not a wider
numeric default. Existing128/512KiB calls and the shared512KiB source/Retainer
limits remain. Full context uses #357's finite1.5MiB decoded/2MiB request contract,
requires the Pre preview, checks all actual request bytes before transport, and
matches actual Pre bytes to its preview. Tools remain empty, store false,
redirects/retries disabled. No alternate model execution loop is introduced.

Quick is built ONLY after the original validated Pre routes CONTINUE_TO_QUICK.
Permission, original source-page checks, pinned stored bytes and decoded/packet
identity are checked again. Its actual request is checked independently; there
is no fabricated future Quick result used to prove capacity. STOP/WAIT do not
call Quick. Provider token-window acceptance and useful Research output are
not certified by either byte check.

## Verification boundaries

New tests use the original fake GitHub/Retainer fixture, real admission and
Funnel validators, real SDK preview and deny transports. They cover both ticker
scopes, STOP/WAIT/conditional Quick, preserved old-history/default-limit guards,
pre-launch failure, changed permission/plain/stored source before Quick, packet
substitution, source-preparation incompleteness and source-clock preservation.
No original test is deleted, loosened or skipped. These companies and outputs
are synthetic fixtures, not real Research outcomes or live source acquisition.

Local editing used only two exact current source modules (original blobs
a841b268 and2dfb3603), plus already audited #357 files. No complete current-main
checkout or functioning pinned SDK was available locally. Local checks were
syntax/AST preservation and the unchanged #357 codec on actual Guangha bytes:
636462 ->221497 ->exact original. New host/SDK tests were NOT run locally;
complete original PR CI, review, main CI and ordinary publication are separate.

## Still required before the two real companies execute

This internal bridge does not activate the production workflow or create a
legal successor. Existing consumed baseline/recovery work remains untouched.
Actual Heshun full context, original page-note/complete source validation, actual
company Pre input identity/request and latest source clocks must be checked.
Guangha's audited636462-byte context is retained, not silently substituted.
A legitimate successor must bind original parent failures, consumed recovery,
source-only34765190284, current fixed reading and existing Human5652950925.
No Re-run, recover-sources=true, rerun of600184/300183, automatic Deep/Odds/Action,
trade/position/monitoring authority follows from this code or a green CI.
