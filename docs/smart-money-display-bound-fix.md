# Smart Money full-record display boundary — 2026-09-26

Continues #351/5841579235 and 5844004077, preserving #586–#591 and #588.
This is a display/observability correction, not new collection or a larger cap.

The original 204-request capture and 22-request repair are retained unchanged.
Exact main 02d30ccd diagnostics36224658079 read existing GitHub inputs without
source/ref writes. Collection returned an optional Smart Money SourceError gap.
Local replay of both exact ZIPs reconstructed137572 records and READY/0 missing
partitions, but the19 canonical gzip chunks total24558019bytes; base64 expansion
plus page data exceeds the original30MiB offline browser cap. This failure must
not hide newly qualified disclosures and quarterly tables.

Reuse original native gzip/base64/SHA256, existing view and publisher. The browser
gets a declared display projection of EVERY retained record, omitting only the
record-ID hash it never consumes. Every other field, source locator and content
version remains. The complete IDs stay in unchanged canonical history chunks;
the display chunks carry their own digest and their original chunk reference.
No new dependency, loader, external host, fee, source call or investment authority.

Keep the30MiB cap. A size-only failure yields a small explicit navigation page
while publishing all qualified canonical data/state. Other integrity errors still
reject the optional composition. Historical withdrawn rows are labelled as such,
not displayed as inferred current holdings or exits. Existing CLI failures gain
only static local transport labels and stage, never raw exception/response text.

Tests must prove exact all-row projection, canonical input immutability, original
and projection digests, size-only isolation and continued identity rejection.
Real original replay/browser size is separate from fullCI, normalmain publisher,
fixedR readback and actual Quick consumption. Temporary diagnostic work stays out
of main and retires at the end; the optional display and tests share its lifecycle.
